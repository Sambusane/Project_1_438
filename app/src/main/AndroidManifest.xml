<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_1_438">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditAccountPage"></activity>
        <activity android:name=".EditAccountLogin" />
        <activity android:name=".AdminEditActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".Display_Page" />
        <activity
            android:name=".tempDisplayPage"
            android:label="Details"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CreateAccountActivity"
            android:label="Details"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".loginPage"
            android:label="Details"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Details"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>